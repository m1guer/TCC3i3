import { createStyles } from '@mantine/styles';

var useStyles = createStyles((theme, { orientation }) => ({
  root: {
    display: orientation === "vertical" ? "flex" : void 0
  }
}));

export default useStyles;
//# sourceMappingURL=Tabs.styles.js.map
