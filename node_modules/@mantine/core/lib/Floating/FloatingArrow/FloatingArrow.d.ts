import React from 'react';
import { FloatingPosition } from '../types';
interface FloatingArrowProps extends React.ComponentPropsWithoutRef<'div'> {
    withBorder: boolean;
    position: FloatingPosition;
    arrowSize: number;
    arrowOffset: number;
    visible: boolean;
}
export declare function FloatingArrow({ withBorder, position, arrowSize, arrowOffset, visible, ...others }: FloatingArrowProps): JSX.Element;
export declare namespace FloatingArrow {
    var displayName: string;
}
export {};
//# sourceMappingURL=FloatingArrow.d.ts.map